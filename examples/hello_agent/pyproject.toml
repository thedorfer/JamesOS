[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jamesos-hello-agent"
version = "1.0.0"

[project.entry-points."jamesos.agents"]
hello = "hello_agent.agent:HelloAgent"

