# JamesOS REST API

Runs on desktop as a user systemd service:

    jamesos-api.service

Default URL:

    http://desktop.local:8787
    http://192.168.5.105:8787

Endpoints:

    GET  /health
    POST /intake
    GET  /search?q=Kevin
    GET  /daily-briefing
    GET  /status-report
