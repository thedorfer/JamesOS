# Changelog

## Unreleased — current web-first implementation

- Implemented the `/app` shell, Home, The Agency registry, Admin, EHF, Product Studio, contextual navigation, health, attachments, and private layouts.
- Repaired local Jade/Ollama response handling and added Private chat plus the Adult-mode implementation and installation policy.
- Added protected Admin configuration, sanitized diagnostics, immutable commerce destinations, deterministic typography artwork, provider-free preflight, and fake-provider unpublished-draft coverage.
- Installed and enabled the JamesOS systemd user service with user lingering on the desktop.

Manual acceptance remains incomplete. Adult mode has an unresolved browser policy-state defect. The latest real Product Studio browser attempt produced zero eligible artwork after `production_artifact_ready`; it created no Printify draft, published nothing, created no Etsy listing, and created no order. Commit `916f14e` is an implementation milestone, not verified real-provider completion.

See [Current status](docs/CURRENT_STATUS.md).
